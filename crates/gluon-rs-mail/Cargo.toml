[package]
name = "gluon-rs-mail"
version = "0.1.0"
edition = "2021"

[dependencies]
gluon-rs-core = { path = "../gluon-rs-core" }
rusqlite = { version = "0.31", features = ["bundled"] }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
